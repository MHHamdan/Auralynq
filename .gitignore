# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Env / secrets
.env
.env.local
*.key

# Data & artifacts (kept out of VCS; reproducible via scripts)
data/
!data/.gitkeep
models/
.cache/
*.qdrant
storage/

# Reports are generated; keep the dir + the canonical artifacts that back the
# README benchmark tables, ignore everything else.
reports/*
!reports/.gitkeep
!reports/eval_report.json
!reports/bench_report.json
!reports/golden_baseline.json

# Node / web
web/node_modules/
web/.next/
web/out/
web/.turbo/

# OS / editor
.DS_Store
.idea/
.vscode/
*.log
